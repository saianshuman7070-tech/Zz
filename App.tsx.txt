/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
 */

import { useState, useCallback, useEffect } from "react";
import { motion, useScroll, useTransform, AnimatePresence } from "motion/react";
import { 
  Copy, 
  Check, 
  Share2, 
  Instagram, 
  ArrowRight, 
  Flame, 
  Sparkles,
  Github,
  Twitter,
  ExternalLink,
  MessageCircle
} from "lucide-react";

// Mock Data
const ART_DATA = {
  title: "Ethereal Chronos",
  description: "A cinematic masterpiece capturing the essence of time in a futuristic cyberpunk metropolis. The light cascades through the holographic mist, creating a dance of colors that defies reality.",
  image: "https://images.unsplash.com/photo-1618005182384-a83a8bd57fbe?q=80&w=2564&auto=format&fit=crop",
  prompt: "A futuristic floating city in the sky, ultra realistic, cinematic lighting, 8K, highly detailed, sci-fi environment, dramatic clouds, cyberpunk aesthetic, holographic neon reflections, volumetric fog, intricate architecture",
  related: [
    {
      id: 1,
      title: "Neon Synthesis",
      image: "https://images.unsplash.com/photo-1605142859862-978be7eba909?q=80&w=2670&auto=format&fit=crop",
      prompt: "Abstract digital landscape, neon lights, grid pattern, retro 80s aesthetic"
    },
    {
      id: 2,
      title: "Solitary Voyager",
      image: "https://images.unsplash.com/photo-1451187580459-43490279c0fa?q=80&w=2672&auto=format&fit=crop",
      prompt: "Spaceship drifting in a colorful nebula, cinematic orbital view, deep space"
    },
    {
      id: 3,
      title: "Emerald Echoes",
      image: "https://images.unsplash.com/photo-1550684848-fac1c5b4e853?q=80&w=2670&auto=format&fit=crop",
      prompt: "Overgrown ancient ruins in a lush jungle, sunbeams, hyper-realistic, 8K"
    }
  ]
};

export default function App() {
  const [copied, setCopied] = useState(false);
  const [activePrompt, setActivePrompt] = useState(ART_DATA.prompt);

  const copyToClipboard = useCallback(async (text: string) => {
    try {
      await navigator.clipboard.writeText(text);
      setCopied(true);
      setTimeout(() => setCopied(false), 2000);
    } catch (err) {
      console.error("Failed to copy!", err);
    }
  }, []);

  const shareOnWhatsApp = () => {
    const text = encodeURIComponent(`Check out this trending AI art: ${window.location.href}`);
    window.open(`https://wa.me/?text=${text}`, "_blank");
  };

  const shareOnInstagram = () => {
    alert("In a real app, this would open Instagram share or creative studio.");
  };

  return (
    <div className="min-h-screen mesh-bg p-4 md:p-6 selection:bg-[#00f2ff] selection:text-black font-sans">
      <div className="max-w-[1400px] mx-auto h-full flex flex-col lg:flex-row gap-4 md:gap-6">
        
        {/* 🖼️ BENTO LONG: MAIN ARTWORK */}
        <motion.div 
          initial={{ opacity: 0, scale: 0.98 }}
          animate={{ opacity: 1, scale: 1 }}
          className="flex-1 relative min-h-[500px] lg:min-h-0 flex flex-col overflow-hidden rounded-[2rem] border border-white/10 shadow-2xl"
        >
          <div className="absolute inset-0 z-0">
            <img 
              src={ART_DATA.image} 
              alt="Hero AI Art" 
              className="w-full h-full object-cover grayscale-[10%] brightness-[0.8]"
              referrerPolicy="no-referrer"
            />
            <div className="absolute inset-0 bg-gradient-to-t from-black/90 via-black/20 to-black/20" />
          </div>

          <div className="relative z-10 mt-auto p-8 md:p-12">
            <motion.div 
              initial={{ opacity: 0, x: -20 }}
              animate={{ opacity: 1, x: 0 }}
              transition={{ delay: 0.2 }}
              className="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-[#00f2ff]/20 border border-[#00f2ff]/30 text-[#00f2ff] text-[10px] font-bold uppercase tracking-[0.2em] mb-6"
            >
              Trending Now <span className="animate-pulse text-white">🔥</span>
            </motion.div>
            
            <motion.h1 
              initial={{ opacity: 0, y: 20 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ delay: 0.3 }}
              className="text-5xl md:text-8xl font-[900] leading-[0.9] mb-6 uppercase tracking-tighter"
            >
              <div className="neon-text italic skew-x-[-10deg]">{ART_DATA.title.split(' ')[0]}</div>
              <div className="text-white">{ART_DATA.title.split(' ')[1]}</div>
            </motion.h1>
            
            <motion.p 
              initial={{ opacity: 0 }}
              animate={{ opacity: 1 }}
              transition={{ delay: 0.4 }}
              className="text-lg md:text-xl text-neutral-300 max-w-xl font-light leading-relaxed mb-8"
            >
              {ART_DATA.description}
            </motion.p>

            <div className="flex flex-wrap gap-4">
               <button className="px-8 py-3 rounded-xl bg-white text-black font-black uppercase tracking-widest text-xs hover:bg-[#00f2ff] transition-all">
                Gallery
               </button>
               <button className="px-8 py-3 rounded-xl glass font-black uppercase tracking-widest text-xs hover:bg-white/10 transition-all flex items-center gap-2">
                Learn More <ArrowRight className="w-4 h-4" />
               </button>
            </div>
          </div>
        </motion.div>

        {/* 🍱 BENTO SIDEBAR */}
        <div className="lg:w-[420px] flex flex-col gap-4 md:gap-6">
          
          {/* 🧠 PROMPT BOX */}
          <motion.div 
            initial={{ opacity: 0, x: 20 }}
            animate={{ opacity: 1, x: 0 }}
            className="glass p-6 md:p-8 rounded-[2rem] flex flex-col gap-6"
          >
            <div className="flex justify-between items-center bg-white/5 -mx-8 -mt-8 px-8 py-4 border-b border-white/5">
              <span className="text-[10px] uppercase tracking-[0.3em] text-neutral-400 font-bold flex items-center gap-2">
                <Sparkles className="w-3 h-3 text-[#00f2ff]" /> Generation Prompt
              </span>
              <span className="text-[9px] bg-[#00f2ff]/10 text-[#00f2ff] px-2 py-0.5 rounded font-black">AI V6.2</span>
            </div>

            <div className="prompt-box p-5 rounded-xl font-mono text-sm leading-relaxed text-cyan-50/70 relative">
              <span className="text-[#00f2ff] opacity-40 mr-2">/imagine</span>
              {activePrompt}
            </div>

            <div className="grid grid-cols-2 gap-3">
              <button 
                onClick={() => copyToClipboard(activePrompt)}
                className="bg-white text-black font-black py-4 rounded-xl text-xs uppercase tracking-widest hover:bg-[#00f2ff] transition-colors flex items-center justify-center gap-2"
              >
                {copied ? <Check className="w-4 h-4" /> : <Copy className="w-4 h-4" />}
                {copied ? "Copied" : "Copy"}
              </button>
              <button className="neon-glow bg-[#00f2ff]/5 text-[#00f2ff] font-black py-4 rounded-xl text-xs uppercase tracking-widest hover:bg-[#00f2ff]/10 transition-colors">
                Generate
              </button>
            </div>
          </motion.div>

          {/* 📲 VIRAL BOX */}
          <motion.div 
            initial={{ opacity: 0, x: 20 }}
            animate={{ opacity: 1, x: 0 }}
            transition={{ delay: 0.1 }}
            className="glass p-6 md:p-8 rounded-[2rem]"
          >
            <span className="text-[10px] uppercase tracking-[0.3em] text-neutral-400 font-bold block mb-6 px-1">Social Distribution</span>
            <div className="flex gap-3">
              <button 
                onClick={shareOnWhatsApp}
                className="flex-1 h-14 glass rounded-2xl flex items-center justify-center hover:bg-white/10 transition-all group"
              >
                <MessageCircle className="w-5 h-5 group-hover:text-[#25D366] transition-colors" />
              </button>
              <button 
                onClick={shareOnInstagram}
                className="flex-1 h-14 glass rounded-2xl flex items-center justify-center hover:bg-white/10 transition-all group"
              >
                <Instagram className="w-5 h-5 group-hover:text-[#E4405F] transition-colors" />
              </button>
              <button 
                onClick={() => copyToClipboard(window.location.href)}
                className="flex-1 h-14 glass rounded-2xl flex items-center justify-center hover:bg-white/10 transition-all group"
              >
                <Share2 className="w-5 h-5 group-hover:text-[#00f2ff] transition-colors" />
              </button>
            </div>
          </motion.div>

          {/* 💡 RELATED BOX */}
          <motion.div 
            initial={{ opacity: 0, x: 20 }}
            animate={{ opacity: 1, x: 0 }}
            transition={{ delay: 0.2 }}
            className="flex-1 glass p-6 md:p-8 rounded-[2rem] flex flex-col"
          >
            <span className="text-[10px] uppercase tracking-[0.3em] text-neutral-400 font-bold block mb-6 px-1">Related Artifacts</span>
            <div className="flex flex-col gap-4 flex-1">
              {ART_DATA.related.map((item) => (
                <div 
                  key={item.id}
                  onClick={() => setActivePrompt(item.prompt)}
                  className="flex gap-4 items-center group cursor-pointer p-2 -m-2 rounded-2xl hover:bg-white/5 transition-colors"
                >
                  <div className="w-16 h-16 rounded-xl overflow-hidden grayscale group-hover:grayscale-0 transition-all border border-white/5">
                    <img src={item.image} className="w-full h-full object-cover" />
                  </div>
                  <div className="flex flex-col gap-1">
                    <span className="text-sm font-bold uppercase tracking-tight group-hover:text-[#00f2ff] transition-colors">{item.title}</span>
                    <span className="text-[10px] text-neutral-500 font-mono">{(Math.random() * 5 + 1).toFixed(1)}k generations</span>
                  </div>
                  <ExternalLink className="w-4 h-4 ml-auto opacity-0 group-hover:opacity-40 transition-opacity" />
                </div>
              ))}
            </div>

            <div className="mt-8 pt-6 flex flex-col gap-4 border-t border-white/5">
              <div className="flex justify-between items-center text-[9px] text-neutral-600 font-bold tracking-widest uppercase">
                <span>AI-Engine Driven</span>
                <span>© 2024 Aura</span>
              </div>
              <div className="flex gap-4">
                <Twitter className="w-4 h-4 text-neutral-600 hover:text-white cursor-pointer" />
                <Github className="w-4 h-4 text-neutral-600 hover:text-white cursor-pointer" />
              </div>
            </div>
          </motion.div>
        </div>
      </div>
    </div>
  );
}
